Note: this example is just for the "Authentication" part of "Auth"

1. npm install
2. open package.json > debug button > start script
3. set breakpoints at the top of login and signup
4. Import postman collection
5. run postman collection requests
